/**:
  ros__parameters:
    # Single-camera counterpart of Autoware's six-camera fusion_common.param.yaml.
    # Every per-camera array must have exactly image_number entries.
    # MORAI bridge messages share simulation time; do not apply the sample-kit offset.
    rois_timestamp_offsets: [0.0]
    rois_timeout_sec: 0.5
    msg3d_timeout_sec: 0.5 #0.05
    image_buffer_size: 15

    point_project_to_unrectified_image: [false]
    filter_scope_min_x: -100.0
    filter_scope_min_y: -100.0
    filter_scope_min_z: -100.0
    filter_scope_max_x: 100.0
    filter_scope_max_y: 100.0
    filter_scope_max_z: 100.0

    approximate_camera_projection: [true]
    approximation_grid_cell_width: 1.0
    approximation_grid_cell_height: 1.0

    debug_mode: false
    collector_debug_mode: false
    publish_processing_time_detail: false

    publish_previous_but_late_output_msg: false
    rosbag_length: 10.0
    matching_strategy:
      type: advanced
      msg3d_noise_window: 0.02
      rois_timestamp_noise_window: [0.05]
